node_modules/
*.log
.DS_Store
.env
.env.*
!.env.example
dist/
dashboard/node_modules/
dashboard/dist/
# public/ contains the built dashboard bundle — committed for now so npm pack
# ships ready-to-serve assets without a build step on the consumer side.
